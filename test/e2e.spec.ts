/*!
 * Copyright (c) 2020 SAP SE or an SAP affiliate company. All rights reserved.
 */

describe('The whole enchilada', () => {
  it('should setup and deploy a new project from scratch', () => {
    expect(true).toBeTruthy();
  });

  it('should setup and deploy an existing express app', () => {
    expect(true).toBe(true);
  });

  it('should setup and deploy an existing nest app', () => {
    expect(true).toBe(true);
  });

  it('should setup and deploy a new project from scratch with approuter', () => {
    expect(true).toBeTruthy();
  });

  it('should setup and deploy an existing express app with approuter', () => {
    expect(true).toBe(true);
  });

  it('should setup and deploy an existing nest app with approuter', () => {
    expect(true).toBe(true);
  });
});
